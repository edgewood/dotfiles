# prefer plain text to HTML in general
message-hook . "unalternative_order *; alternative_order text/plain text/html"

# drop plain text from senders who send an empty, trivial, or ugly plain text part
message-hook "%f htmlsenders"	"unalternative_order text/plain"

